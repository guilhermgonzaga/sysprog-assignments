# Set the default behavior, in case core.autocrlf is not set.
* text eol=lf

# Denote all files that are truly binary and should not be modified.
*.a   binary
*.o   binary
*.out binary

buildimage/bin/buildimage.given binary

# Define header files to be considered C language, rather than C++, by linguist.
*.h linguist-language=C
